# Design QA

## Source visual truth

- Path: `/var/folders/gr/j7hpdpf93qn19xl2w1s2gswc0000gn/T/codex-clipboard-14e7aee4-ff5b-4701-960f-d4fbe98da27c.png`
- Pixel dimensions: 326 x 56
- Intended state: 참고 링크가 `장소명 | 서비스명` 메타데이터 제목과 파비콘으로 표시되는 상태

## Implementation evidence

- Path: `/private/tmp/aloc-local-implementation.png`
- Pixel dimensions: 1280 x 963
- CSS viewport: 1280 x 963; density normalization: none
- Captured state: 로컬 장소 추천 입력 화면

## Comparison

The source crop and implementation capture do not represent the same route or state. The local app could be opened and rendered, but the detail screen requires an authenticated, completed recommendation-history response that was not available in the local session. Therefore the 참고 링크 region could not be captured for a like-for-like visual comparison.

- Fonts and typography: not comparable; the target link region was not rendered.
- Spacing and layout rhythm: not comparable; the target link region was not rendered.
- Colors and visual tokens: not comparable; the target link region was not rendered.
- Image quality and asset fidelity: the implementation uses a real favicon URL rather than a CSS or inline drawing; visual fidelity was not directly verifiable in the detail state.
- Copy and content: the pure display-title path is covered by `referenceLinks.test.ts`, including `선이네수제순대국 | 카카오맵` fallback behavior.

## Findings

- [P1] Visual comparison blocked. Location: `PlaceRecommendationResultItemDetail` 참고 링크 section. Evidence: the implementation screenshot is the input form, not the detail route shown by the source. Impact: final visual fidelity of the link row cannot be confirmed. Fix: open a completed recommendation detail route in an authenticated local session and recapture the same region.

## Comparison history

- Initial pass: blocked because the authenticated completed-result detail state was unavailable. No visual fixes were applied from this pass.

## Implementation Checklist

- [x] Fetch page metadata through the existing link-metadata API when the detail view loads.
- [x] Use the page title when it is specific.
- [x] Use `장소명 | 사이트명` when the metadata is empty, fails, or only reports the generic site name.
- [x] Keep favicon rendering and external-link behavior.
- [ ] Re-run focused visual comparison on the completed detail route.

## Follow-up Polish

- Verify the remote favicon provider returns the Kakao/ Naver brand icon in the deployed environment; if it falls back to a generic globe, expose the source site's favicon URL from the metadata endpoint.

final result: blocked
