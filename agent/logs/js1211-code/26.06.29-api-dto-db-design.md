# API DTO & DB 설계

- 작성자: js1211-code
- 작성일: 2026-06-29
- 상태: 검토 요청

---

## 1. DB 설계

### 테이블 관계 요약

```
users (1) ──< recommendation_histories (1) ──< saved_places
                                          └──< (saved_places.history_id)
```

### 1-1. `users`

유저 계정 정보.

| 컬럼 | 타입 | 제약 | 설명 |,
|------|------|------|------|
| `id` | UUID | PK, DEFAULT gen_random_uuid() | 유저 식별자 |
| `email` | TEXT | UNIQUE, NOT NULL | 로그인용 이메일 |
| `password_hash` | TEXT | NOT NULL | bcrypt 해시 |

---

### 1-2. `recommendation_histories`

추천 엔진 실행 기록. 로그인 유저와 비로그인 유저 모두 저장 가능.

| 컬럼 | 타입 | 제약 | 설명 |
|------|------|------|------|
| `id` | UUID | PK, DEFAULT gen_random_uuid() | 기록 식별자 |
| `user_id` | UUID | NULL, FK → users.id | 로그인 유저면 연결, 비로그인이면 NULL |
| `input` | JSONB | NOT NULL | `UserInput` 스키마 그대로 저장 |
| `output` | JSONB | NULL | `UserOutput` 스키마 그대로 저장. 에러면 NULL |
| `error_message` | TEXT | NULL | 엔진 에러 시 메시지 |
| `created_at` | TIMESTAMPTZ | NOT NULL, DEFAULT now() | 요청 일시 |

> **input/output 타입**: `packages/recommendation-engine`의 `UserInput`, `UserOutput` Zod 스키마 기준.

---

### 1-3. `saved_places`

유저가 추천 결과에서 저장(즐겨찾기)한 장소.

| 컬럼 | 타입 | 제약 | 설명 |
|------|------|------|------|
| `id` | UUID | PK, DEFAULT gen_random_uuid() | 저장 식별자 |
| `user_id` | UUID | NOT NULL, FK → users.id | 저장한 유저 |
| `history_id` | UUID | NULL, FK → recommendation_histories.id | 어느 추천 세션에서 저장했는지 (선택) |
| `place_data` | JSONB | NOT NULL | `PlaceRecommendationItem` 스키마 그대로 저장 |
| `created_at` | TIMESTAMPTZ | NOT NULL, DEFAULT now() | 저장 일시 |

> **중복 저장 방지**: 동일 유저가 같은 장소(place_data.id 기준)를 중복 저장하지 못하게 unique 제약 검토 필요.
> → `UNIQUE(user_id, (place_data->>'id'))` 형태의 표현식 인덱스로 구현 가능.

---

## 2. API DTO 설계

### 공통 응답 형태

기존 `packages/api-contracts`의 `ApiResponse<T>` 래퍼를 그대로 사용.

```ts
// 성공
{ success: true, data: T }

// 실패
{ success: false, error: string }
```

---

### 2-1. 유저 관련

#### `POST /api/auth/signup` — 회원가입

**요청 Body**

```ts
{
  email: string;      // 이메일 형식
  password: string;   // 최소 8자
}
```

**응답 `data`**

```ts
{
  user: {
    id: string;
    email: string;
  };
  token: string;      // JWT access token
}
```

**에러 케이스**

| HTTP | error 메시지 예시 | 설명 |
|------|------|------|
| 400 | `"invalid input"` | 유효성 검사 실패 |
| 409 | `"email already exists"` | 이메일 중복 |

---

#### `POST /api/auth/login` — 로그인

**요청 Body**

```ts
{
  email: string;
  password: string;
}
```

**응답 `data`**

```ts
{
  user: {
    id: string;
    email: string;
  };
  token: string;      // JWT access token
}
```

**에러 케이스**

| HTTP | error 메시지 예시 | 설명 |
|------|------|------|
| 400 | `"invalid input"` | 유효성 검사 실패 |
| 401 | `"invalid credentials"` | 이메일/비밀번호 불일치 |

---

#### `GET /api/users/me` — 내 정보 조회

> Authorization: `Bearer {token}` 헤더 필요 (이하 인증 필요 엔드포인트 동일)

**응답 `data`**

```ts
{
  user: {
    id: string;
    email: string;
  };
}
```

---

#### `DELETE /api/users/me` — 회원 탈퇴

**응답 `data`**

```ts
{
  success: true;
}
```

---

### 2-2. 추천 기록 관련

#### `GET /api/histories` — 내 추천 기록 목록

**쿼리 파라미터**

```
?limit=20&offset=0
```

**응답 `data`**

```ts
{
  items: {
    id: string;
    input: UserInput;               // 요청 당시 입력값
    recommendationCount: number;    // output 안의 추천 결과 수
    createdAt: string;              // ISO 8601
  }[];
  total: number;
}
```

> `output` 전체를 내려주면 너무 무거우므로, 목록에서는 `recommendationCount`만 내려줌.
> 상세 조회가 필요하면 별도 엔드포인트(`GET /api/histories/:id`) 추가 검토.

---

### 2-3. 장소 저장 관련

#### `POST /api/saves` — 장소 저장

**요청 Body**

```ts
{
  placeData: PlaceRecommendationItem;   // 엔진 output의 장소 객체 그대로
  historyId?: string;                   // 어느 추천 세션에서 저장했는지 (선택)
}
```

**응답 `data`**

```ts
{
  saved: {
    id: string;
    placeData: PlaceRecommendationItem;
    createdAt: string;
  };
}
```

**에러 케이스**

| HTTP | error 메시지 예시 | 설명 |
|------|------|------|
| 400 | `"invalid input"` | 유효성 검사 실패 |
| 409 | `"already saved"` | 같은 장소를 이미 저장함 |

---

#### `GET /api/saves` — 저장 목록 조회

**쿼리 파라미터**

```
?limit=20&offset=0
```

**응답 `data`**

```ts
{
  items: {
    id: string;
    placeData: PlaceRecommendationItem;
    createdAt: string;
  }[];
  total: number;
}
```

---

#### `DELETE /api/saves/:id` — 저장 삭제

**응답 `data`**

```ts
{
  success: true;
}
```

**에러 케이스**

| HTTP | error 메시지 예시 | 설명 |
|------|------|------|
| 403 | `"forbidden"` | 본인이 저장한 항목이 아님 |
| 404 | `"not found"` | 존재하지 않는 항목 |

---


의논해야할것

1. **JWT 만료 시간**: access token만 쓸지, refresh token도 둘지?
2. **비로그인 추천 기록 저장**: 비로그인 상태의 추천 기록(`user_id = NULL`)을 DB에 저장할 필요가 있는지? (서버 로그만으로 충분한지)
3. **`GET /api/histories/:id` 상세 조회**: 필요한지 → 프론트 UI 설계에 따라 결정
4. **중복 저장 방지 정책**: 같은 장소 중복 저장 시 409 에러 vs 덮어쓰기 vs 허용 중 어느 방식?
