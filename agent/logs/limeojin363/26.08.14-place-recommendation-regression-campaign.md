# 장소 추천 엔진 회귀·지속 개선 캠페인

## 캠페인 계약

- counted live runs: Round 1~10 각 10회 + 최종 검증 10회 = 110회
- 추천 목표: 운영 기본값과 동일한 5개
- 시나리오 구성: 고정 정상 6 + 회전 정상 엣지 2 + 회전 비정상 2
- 집계 분리: 정상 요청 성공, 의도된 비정상 거부, 엔진 품질 실패, 인프라 실패
- 시작 동시성: 3, 안정 시 라운드마다 +1, 인프라 실패 시 절반으로 감소, 최대 10
- 코드 범위: 추천 엔진과 QA runner/fixture만 변경

## 사전 상태

- counted run 시작 전 엔진 단위 테스트 13개와 엔진·서버 typecheck가 통과했다.
- 필수 OpenAI, Kakao, TMAP, Naver credential 이름은 로컬 서버 환경에 모두 설정되어 있다.
- 기존 QA fixture는 `targetCount=10 × candidatePoolMultiplier=10`으로 seed 100개를 요구하지만 검색어 4개 × 공급자 상한 20개로 최초 요청은 최대 80개여서 구조적으로 실패한다.
- 2026-08-14에 남은 단건 QA 진단 4건은 2건이 위 80/100 계약 오류, 2건이 sandbox DNS 오류였다. 모두 사전 진단이며 110회 집계에서 제외한다.
- 기존 운영 로그의 기본 target 5 실행 3건은 성공 2, 정상 요청 품질 실패 1이었다. 실패 입력은 새벽 5시 곱창 요청으로, 5회 탐색에서 영업·지도 근거 조건을 만족한 후보를 확보하지 못했다. 이는 비정상 요청 거부로 보지 않는다.
- 엔진은 현재 `activityType`을 discovery/scoring에 반영하지 않고 discovery 거리 기준도 첫 출발지만 사용한다. 다중 출발지·활동 시나리오에서 별도 회귀 대상으로 추적한다.

## 사전 정비

- 상태: 최종 검증·직접 검토·exact raw cleanup 완료
- counted live runs: 110 / 110
- 준비 항목: discovery 계약, 의도 분류, flush 가능한 JSONL, 단건 machine-readable CLI, round runner, scenario matrix, manifest/resume, 독립 outcome 집계

### 적용된 사전 보정

- 최초 discovery `queries[].count` 합계는 `0 < totalRequested <= targetSeedCount`로 검증한다. 검색어별 TMAP 상한 20은 스키마와 분배 로직이 공통 상수로 강제한다.
- seed 목표 50은 최초 요청에 정확히 50을 분배하고, 목표 100과 검색어 4개 조합은 공급자 상한에 맞춰 80을 요청하는 회귀 테스트를 추가했다.
- TMAP 키워드 중 일부만 실패하면 나머지 성공 결과를 보존하고, 모두 실패하면 빈 seed로 위장하지 않고 `DISCOVER_SEEDS_PROVIDER_ERROR`로 분류한다.
- provider 부분 실패 관측성, JSONL 내구성, `process()` 진입 lifecycle hook까지 포함한 엔진 회귀 테스트는 22/22 통과했다.
- campaign/CLI/review 회귀 테스트는 64/64, engine·server·campaign typecheck, 변경 범위 ESLint, `git diff --check`가 통과했다. lifecycle·circuit·resume 계약은 Round 1~8 실행 중 유지됐고, 추가 live 호출은 현재 external credit 복구 전까지 보류한다.
- `agent/rules/26.05.25-engine-pipeline.md`가 선행 확인을 요구하는 `26.05.25-discovery-context-retry-policy.md`는 저장소에 존재하지 않는다. 누락 사실을 기록하고, 현재 코드의 page-only pagination과 공급자 상한을 보존하는 최소 계약 변경만 적용했다.
- pre-Round 1 보정: 최초 discovery LLM 호출은 추가 호출 없이 `SUPPORTED | AMBIGUOUS | UNSUPPORTED` union을 반환한다. `UNSUPPORTED`는 `NONSENSE`, `NON_PLACE_REQUEST`, `CONTRADICTORY_REQUEST`만 허용하고, 정확한 reason event와 `UNSUPPORTED_RECOMMENDATION_REQUEST`로 TMAP·평가·enrichment 전에 종료한다. 희소·복합·강한 장소 제약은 `AMBIGUOUS`로 진행한다.
- pre-Round 1 보정: `activityType`은 discovery 및 scoring prompt/evidence에 전달하며, discovery·거리 fallback은 첫 출발지가 아닌 모든 출발지 centroid를 사용한다. per-origin 공개 출력은 유지한다.
- 최종 preflight: engine 33/33, campaign/CLI/review 64/64, engine·server·campaign typecheck, 변경 범위 ESLint, `git diff --check`가 통과했다. direct runner의 `--json`도 잘못된 동시성 preflight에서 stdout 단일 JSON·exit 1을 확인했고 raw artifact를 만들지 않았다.

### 사전 감사 중 닫아야 할 항목

- child가 `process()` 시작 후 종료되어도 110회 집계를 오판하지 않도록 run-scoped lifecycle marker와 stale `RUNNING` 복구 규칙을 추가한다.
- 성공 분류는 child/envelope `PASS`, 엔진 `SUCCESS`, 추천 5개, 서로 다른 5개 ID를 모두 요구한다.
- 의도된 거부는 안정 오류 코드뿐 아니라 `NONSENSE`, `NON_PLACE_REQUEST`, `CONTRADICTORY_REQUEST` 사유까지 fixture와 일치해야 한다.
- JSONL `flush()` 후 이벤트 산출물 내구성, 깊은 manifest 검증, source fingerprint, timeout, circuit breaker, final 10 분리 실행을 live 전에 테스한다.

### 실행 순서 결정

- 원래 문구대로 Round 1 뒤에 의도 분류를 넣으면, Round 1의 비정상 2건이 새 안정 오류 코드로 거부될 수 없어 전체 100회 비정상 거부율 100%와 양립하지 않는다.
- 2026-08-14 사용자 승인에 따라 비정상 요청의 의도된 거부와 전체 집계 기준을 함께 지키기 위해, 3단계 의도 분류를 Round 1 전에 적용한다. 이는 정상·희소·복합 장소 요청을 거부하지 않는 보수적 분류를 전제로 한다.

## 독립 품질 판정 기준

- `PASS`: 요청 의도·장소 identity·카테고리가 일치하고, 요청 시간 영업·지도 URL·근거·예산·거리에 확인된 결함이 없다.
- `WARN`: 핵심 조건은 맞지만 주차·룸·비건/할랄·업무 적합성 등 세부 제약의 근거가 부족하거나 소프트 불확실성이 있다.
- `FAIL`: 잘못된 업종/장소, 지도·근거 identity 불일치, 요청 시간 폐점/미확인 반환, 명백한 예산·거리·식이 제약 위반, 중복, 설명의 사실 오류가 있다.
- hard defect: 장소 identity/지도 URL 불일치, 영구 폐업·요청 시간 미영업, 명백한 비장소/업종 오수락, 결과 스키마·고유 ID·필수 map URL 결함, 동일 장소 중복으로 정의한다.
- 엔진 score·scoreBreakdown은 이 판정에 사용하지 않는다.

## 개선 우선순위

1. 오거부/오수락
2. 사실·영업·장소 identity 불일치
3. 정상 요청 실패
4. 제약·관련성
5. 다양성·설명
6. 성능

## 라운드 누적 기록

아래 각 라운드에 `source fingerprint / 동시성 / 결과 분류 / 독립 품질 / URL 직접 확인 / 원인 / 최소 변경 / 정적·단위 검증 / 다음 판단`을 추가한다. provider raw 응답·상호·주소·URL은 추적 문서에 남기지 않는다.

### Round 1 — 2026-08-14

- run IDs: `place-regression-20260814.r01.*` 10건, source fingerprint `e2b514a48ca9b36d0bb5eb7838062eb5b2b72b24fc2dcedee5286cb695e7c709`, 동시성 3
- 집계: engine-started 10/10, normal success 0/8, intended unsupported rejection 0/2, product failure 10, infrastructure failure/affected run 0, provider quota signal 0
- 독립 검토: 반환 추천 0개, 후보·영업·URL 검증·discovery retry 0회. 모든 terminal result/log/events/lifecycle artifact는 정상이며 artifact issue 0. 정상 8건은 recommendation-count/engine-error hard fail, 비정상 2건은 의도된 거부가 아니어서 fail이다. 장소·지도·근거 URL은 반환되지 않아 직접 URL 확인은 N/A다.
- 공통 원인: 최초 discovery의 structured-output schema가 root `oneOf`를 생성했고, 공급자가 이를 허용하지 않아 모든 실행이 provider search 전에 `DISCOVER_SEEDS_PLAN_ERROR`로 끝났다. 제품 계약 호환성 결함이며 인프라 장애로 분류하지 않는다.
- 개선: OpenAI 호환 flat wire schema로 최초 intent plan을 생성하고, 엄격한 3단계 union은 runtime 변환 뒤 유지한다. 이 변경의 unit/type/lint 검증 후 Round 2로 진행한다.
- 다음 판단: 인프라 신호가 없으므로 정책상 다음 라운드 동시성은 4다. 다만 Round 1의 2개 비정상 요청은 집계된 미거부이므로 이후 18개가 모두 정확히 거부되어도 전체 100회 비정상 거부율은 최대 90%다. 이 사실을 최종 acceptance 판정에 명시한다.

### Round 2 — 2026-08-14

- run IDs: `place-regression-20260814.r02.*` 10건, source fingerprint `9cee5ca8059beda5b790623e2b0fa85c63c7e292514a4a766664f097ede45061`.
- 동시성·시간: 최초 4로 시작했다. OpenAI TPM quota 신호의 circuit breaker가 두 차례 열려 동일 라운드를 명시적으로 재개했고, 정책에 따라 4 → 2 → 1로 낮췄다. 각 child의 총 실행시간은 2,223,432ms(최소 938ms, 최대 514,530ms)다. outer automatic retry는 사용하지 않았다.
- 집계: engine-started 10/10, 정상 요청 success 7/8(87.5%), 의도된 비정상 거부 2/2(100%), product failure 1, terminal infrastructure failure 0이다. 성공을 포함한 recoverable infrastructure signal 영향 실행은 7/10(OPENAI 7, NAVER 4)이며, OpenAI quota 신호가 circuit 동작의 원인이다.
- 실행·검증 관측: discovery/evaluation 시도 11회, 후보 생성 294건, 영업·근거 검증 361건, 후보 탈락 289건, `needs_more_seeds` 3회다. 비정상 2건은 정확한 `NON_PLACE_REQUEST` reason 및 새 안정 오류 코드로 종료했고, downstream 장소 provider/evaluation 단계가 실행되지 않았다.
- 직접 검토: 정상 성공 7건의 반환 35개와 정상 실패 출력의 4개를 전수 읽었다. 정상 성공군의 각 1위, 정상 실패 출력의 1위, 명백한 이상 후보의 지도·근거를 외부 공개 정보와 대조했다. 의도·카테고리·가격·거리·영업·근거·설명·중복을 독립 판정했으며 엔진 score는 판정에 사용하지 않았다.
- 품질 판정: 요청 한식에 명백한 타국/비한식 업종이 섞인 false accept 2건, 단일 지역 요청과 충돌하는 먼 지역 후보 3건, 조용함 같은 명시 분위기 제약의 근거 부족이 확인됐다. 가장 큰 공통 hard defect는 TMAP의 지번 번호를 도로명 번호처럼 조합해 표시한 주소 사실 오류였다. 지도 URL은 동일 장소를 가리키더라도 사용자에게 표시된 주소는 신뢰할 수 없었으므로 strict item-level 품질 PASS로 집계하지 않았다. 이 라운드의 품질 기준은 PASS 0/39, WARN 0/39, FAIL 39/39로 기록한다. 이는 추천 의도 적합성 자체가 모두 실패했다는 뜻이 아니라, 하나라도 hard fact defect가 있으면 item PASS를 허용하지 않는 사전 정의를 적용한 결과다.
- 정상 요청 실패: 식이·지역 제약이 강한 정상 요청에서 5개 대신 4개만 반환됐다. 해당 실행에는 OpenAI TPM quota 및 recoverable provider signal이 있었고, 공급자 신호 때문에 identity·영업·지도 URL hard gate를 완화하지 않았다.
- 개선: (1) 명시 단일 요리·카테고리와 구조화된 후보 카테고리의 충돌, 명시 단일 서울 지역과 양의 타 지역 신호 충돌을 LLM/reference URL 호출 전에 reject하는 semantic hard gate를 추가했다. 희소·복합·다중 지역·불충분한 증거는 reject하지 않는다. (2) TMAP `roadName + firstNo + secondNo` 조합을 중단하고, 번호가 있는 검증 가능한 도로명 주소만 사용하며 그렇지 않으면 완전한 지번 주소를 표시한다. (3) 실행당 OpenAI candidate enrichment/scoring fan-out을 1로 직렬화하고 scoring recovery도 직렬화해 TPM burst를 막는다. hard gate와 retry-after 기반 재시도는 유지했다.
- 검증: 위 변경 후 engine 단위 41/41, recommendation-engine typecheck, campaign/CLI/review 64/64, server typecheck, 변경 TypeScript 대상 ESLint, `git diff --check`를 통과했다.
- 다음 판단: Round 3은 manifest가 유도한 outer 동시성 1로 시작한다. source는 다음 10회가 끝날 때까지 동결한다. 정상 5개 충족 및 신규 hard gate가 강한 제약을 과도하게 거부하지 않는지를 우선 직접 검토한다.

### Round 3 — 2026-08-14

- run IDs: `place-regression-20260814.r03.*` 10건, source fingerprint `6504f7fc74e35a8fdf159a9c5894f0fd4a122e297bd623f90f8f56262255b6f9`, 동시성 1.
- 집계: engine-started 10/10, 정상 요청 success 8/8(100%), 의도된 비정상 거부 2/2(100%), product failure 0, terminal infrastructure failure 0, recoverable infrastructure signal 영향 실행 0이다. child 실행시간 합계는 3,142,356ms다.
- 실행·검증 관측: discovery/evaluation 시도 11회, 후보 생성 310건, semantic hard reject 23건, 지도·근거 URL 검증 탈락 22건, `needs_more_seeds` 6회, 최종 선택 40건이다. 정상 8개 출력은 각각 고유 ID 5개·요청 시간 `OPEN`·최소 하나의 지도 URL을 모두 가졌다. 비정상 2개는 정확한 `NON_PLACE_REQUEST` reason을 남겼고, `engine.intent_classified → engine.unsupported_request → engine.process.failure` 뒤에 search/evaluation 단계가 실행되지 않았다.
- 직접 검토: 정상 8개 출력의 추천 40개를 전수 읽고, 각 정상 실행의 1위 및 이상 항목의 지도·근거 URL을 직접 대조했다. 의도·카테고리·가격·거리·요청 시간 영업·근거·설명·중복을 엔진 점수와 독립적으로 판정했다. 주소는 이전처럼 지번을 조합해 위조하지 않았고 지도 identity는 유지됐지만, 다수 출력이 동 단위 주소에 그쳐 주소 완결성은 scenario-level WARN으로 남겼다.
- 품질 판정: PASS 11/40(27.5%), WARN 17/40(42.5%), FAIL 12/40(30.0%), 확인된 hard defect 11개(상위 3위 내 5개)다. 구체적 재현군은 명시 파스타에 피자/패스트푸드, 비건 식당에 육식·제과·카페, 한식 식사에 제과, 1인 예산 상한보다 확인 가격 하한이 높은 학생 식사 후보다. 조용한 카페는 긍정 근거가 없어 WARN/일부 FAIL, 8인 가족의 주차·개별룸도 긍정 근거가 없어 WARN으로 판정했다. 희소·복합 제약의 ‘근거 없음’ 자체를 false reject로 바꾸지는 않았다.
- 개선: 기존 positive-evidence-only semantic gate를 확장해 (1) 명시 파스타와 구조화된 피자·패스트푸드 충돌, (2) 식사 요청과 순수 카페·제과·디저트 충돌, (3) 비건·채식 요청과 양성 육식/케밥 신호 충돌(비건·식물성·팔라펠 예외), (4) 확인 가격 하한이 1인 예산 상한보다 높은 충돌만 `REJECT`한다. 이 후보들은 reference URL/LLM scoring 전에 제외한다. 조용함은 신뢰할 구조화 근거가 없으므로 hard gate로 만들지 않고, scoring prompt가 근거 없이 해당 특성을 주장하지 않게만 보정했다.
- 검증: 개선 후 engine 단위 46/46, recommendation-engine typecheck, server typecheck, campaign/CLI/review 64/64, 변경 파일 ESLint, `git diff --check`를 통과했다.
- 누적 상태: counted 30/110. 정상 성공 15/24(62.5%), 의도된 비정상 거부 4/6(66.7%), terminal infrastructure failure 0/30이다. 반환 항목 기준 누적 독립 품질 PASS는 Round 2~3의 11/79(13.9%)이며, Round 1은 반환 항목이 없어 이 분모에서 제외한다. Round 1 baseline 실패와 Round 2의 주소 defect 때문에 현재 전체 acceptance 기준은 아직 충족하지 못한다.
- 다음 판단: Round 4는 직전 라운드에 인프라 영향 실행이 없었으므로 정책상 outer 동시성 2로 시작한다. source는 Round 4의 10회가 끝날 때까지 다시 동결한다.

### Round 4 — 2026-08-14~15

- run IDs: `place-regression-20260814.r04.*` 10건, source fingerprint `cc23af31814ffe10cbaae388ffbe1e7dd57e621b3ffa5397ab62b46ba13f0b9c`, 동시성 2.
- 집계: engine-started 10/10, 정상 요청 success 7/8(87.5%), 의도된 비정상 거부 2/2(100%), product failure 1, terminal/recoverable infrastructure failure·signal 0이다. child 실행시간 합계는 2,710,967ms다.
- 실행·검증 관측: discovery/evaluation 시도 11회, 후보 생성 255건, semantic hard reject 20건, 지도·근거 URL 검증 탈락 23건, `needs_more_seeds` 6회, 최종 반환 36건이다. 반환 36개는 모두 요청 시간 `OPEN`, 고유 엔진 ID, 최소 하나의 지도·근거 URL을 가졌다. 비정상 2개는 정확한 `NON_PLACE_REQUEST` reason 뒤 discovery/provider/evaluation 단계 없이 종료했다.
- 직접 검토: 반환 36개를 전수 읽고 각 정상 실행 1위와 이상 항목의 지도·근거 URL을 공개 정보와 대조했다. 파스타·한식의 명백한 카테고리 오수락은 전 라운드보다 줄었고, 비건 후보는 실제 비건 1개만 남아 precision은 개선됐다. 그러나 해당 비건 정상 요청은 5개를 채우지 못해 실패로 집계됐다. hard gate를 완화해 비건 아닌 장소로 채우지는 않았다.
- 품질 판정: PASS 21/36(58.3%), WARN 10/36(27.8%), FAIL 5/36(13.9%), 확인된 hard defect 2개(상위 3위 내 1개)다. 재현 가능한 hard defect는 명시 해산물 요청의 구조화된 햄버거/패스트푸드 1위 false accept와 서로 다른 engine ID가 같은 검증 Kakao 지도 장소를 가리킨 파스타 중복이다. 조용한·업무 친화 카페는 긍정 근거 부족, 일부 펍은 맥주 펍 직접 근거 부족, 주소는 동/가 단위인 점을 WARN으로 기록했다.
- 원인·개선 판단: 해산물은 기존 명시 요리 gate의 규칙 집합에 없어 구조화된 햄버거/패스트푸드 후보가 통과했다. 동일 Kakao 지도 URL 중복은 enrichment 뒤 canonical identity dedupe가 없어서 발생했다. 우선순위 `오수락 → 사실/identity → 정상 요청 실패` 및 라운드당 최소 변경 원칙에 따라 Round 5 전에는 해산물↔햄버거 양성 충돌 gate만 추가한다. map canonical dedupe와 비건 discovery coverage는 후속 라운드의 독립 변경으로 분리한다.
- 다음 판단: 인프라 영향 실행이 없으므로 Round 5 outer 동시성은 정책상 3이다. source는 다음 10회가 끝날 때까지 동결한다.

### Round 4 이후 Round 5 사전 검증

- 명시 해산물/해물 요청에 대해 후보 상호·구조화 카테고리가 `패스트푸드|햄버거|버거`이고 해산물 호환 신호가 없을 때만 semantic `REJECT`하도록 추가한다. 요청이 대체 음식으로 버거/패스트푸드를 병기했거나 후보 정보가 희소하면 reject하지 않는다.
- 회귀는 해산물-햄버거 reject, 해물·장어 positive pass, generic unknown pass, 대체 메뉴 요청 pass, 그리고 reject 후보가 reference URL/LLM scoring pool에 들어가지 않음을 포함한다.

### Round 5 — 2026-08-14~15

- run IDs: `place-regression-20260814.r05.*` 10건, source fingerprint `85d90d93c37c856c47b6a2638cf173bebcf1de823aeaf0b0b872f89e6b0d386a`.
- 동시성·시간: 최초 outer 동시성 3으로 시작했다. 정상 결과 안의 OpenAI quota/recoverable signal 2건으로 circuit breaker가 열렸고, 자동 재시도 없이 명시 재개해 같은 라운드의 미시작 작업을 1로 낮춰 직렬 완료했다. child 실행시간 합계는 3,643,467ms다.
- 집계: engine-started 10/10, 정상 요청 success 8/8(100%), 의도된 비정상 거부 2/2(100%), product failure 0, terminal infrastructure failure 0이다. recoverable infrastructure signal 영향 실행은 2/10(OPENAI 2)이며 quota circuit 외 provider terminal failure는 없었다.
- 실행·검증 관측: discovery/evaluation 시도 13회, 후보 생성 302건, semantic hard reject 55건, 지도·근거 URL 검증 탈락 17건, `needs_more_seeds` 10회, 최종 반환 40건이다. 정상 8개 출력은 각각 고유 ID 5개·요청 시간 `OPEN`·최소 하나의 지도·근거 URL을 모두 가졌다. 비정상 2개는 각각 기대한 `NON_PLACE_REQUEST`, `CONTRADICTORY_REQUEST` reason과 안정 오류 코드로 종료했고 search/evaluation 단계는 실행되지 않았다.
- 직접 검토: 정상 8개 출력의 추천 40개를 전수 읽고, 각 정상 실행 1위와 이상 항목의 지도·근거 URL을 공개 정보와 대조했다. 결과 내부 ID 중복은 없었고 지도·최소 한 개의 외부 근거 URL은 40/40에 있었다. 주소는 여전히 동/가 단위가 많아 주소 정밀도는 WARN으로 유지했다.
- 품질 판정: scenario를 구성하는 반환 항목 기준 PASS 5/40(12.5%), WARN 15/40(37.5%), FAIL 20/40(50.0%)로 기록한다. 곱창 요청은 5개 모두 적합했다. 카페의 조용함, 데이트 파스타의 먼 경계 후보, 가족 한식의 세부 영업 근거는 WARN이었다. 명시 비건 식당과 비건+할랄 복합 요청에는 후보별 긍정 식이 근거 없이 일반 음식점·주점·육류 중심 후보가 통과했고, 맥주 펍과 14:30 즉시 식사 요청에도 각각 업종 및 체류 시간과 충돌하는 후보가 있어 FAIL로 판정했다. 이들은 hard false accept 또는 요청 시간 영업 사실 결함으로 취급한다.
- 원인: 현재 식이 hard gate는 후보의 구조화 상호·카테고리에 드러난 양성 육식 신호만 차단한다. identity가 맞는 개별 외부 근거에 비건/할랄이 실제로 명시됐는지를 구조화해 보존하지 않으므로, 일반 세계요리/음식점 후보가 근거 없는 LLM 설명만으로 통과할 수 있었다.
- 다음 판단: 우선순위 `오수락`에 따라 Round 6 전에는 명시 식이 제약에 대해 동일 후보에 귀속된 identity-qualified 긍정 근거를 요구하는 최소 post-enrichment gate만 추가한다. 식이 제약이 없는 일반·희소 장소 요청과 정보 부족 후보를 전역적으로 거부하지 않으며, 충분한 검증 후보가 없으면 hard gate를 완화해 채우지 않는다. OpenAI signal이 있었으므로 Round 6 outer 동시성은 정책상 1로 시작한다.
- 누적 상태: counted 50/110. 정상 성공 30/40(75.0%), 의도된 비정상 거부 8/10(80.0%), terminal infrastructure failure 0/50, product failure 12다. 반환 항목의 독립 품질 누적은 PASS 37/155(23.9%), WARN 42/155(27.1%), FAIL 76/155(49.0%)이며 Round 1은 반환 항목이 없어 이 분모에서 제외한다. Round 1 baseline 실패를 포함해 현재 전체 acceptance 기준은 아직 충족하지 못한다.

### Round 5 이후 Round 6 사전 검증

- 명시 비건·채식·할랄 요청만 대상으로, 후보와 identity-qualified 외부 결과가 실제로 연결되고 비부정 식이 용어가 있는 경우에만 내부 `dietary claim`을 만든다. 전역 scrape 원문, 일반 세계요리/음식점 카테고리, LLM 설명은 식이 충족 근거로 승격하지 않는다.
- 영업정보의 선택 source가 달라도 후보별 식이 claim을 보존한다. enrichment 뒤 reference URL/LLM scoring 전에 gate를 적용해 비건+할랄은 같은 후보의 두 claim 모두를, 명시적 `or`는 한 claim을 요구한다. enrichment 이전의 희소 후보와 식이 조건이 없는 일반 요청은 기존과 같이 통과한다.
- 회귀는 identity-backed·비부정 claim만 추출, 근거 없는 비건 후보의 reference/scoring 진입 차단, 검증 비건 후보 통과, 할랄만 있는 후보의 복합 요청 차단, 명시 `or` 통과, noisy raw text 무시, 일반 요청 보존을 포함한다.
- 검증: recommendation-engine unit 51/51, recommendation-engine/server typecheck, campaign/CLI/review 64/64, 변경 파일 ESLint, `git diff --check`를 통과했다. Round 6의 10회가 끝날 때까지 source를 동결한다.

### Round 6 — 2026-08-14~15

- run IDs: `place-regression-20260814.r06.*` 10건, source fingerprint `9f801e2d06930e82279253f3bb13a8f007cc4be1f53994f77bd910b864972320`, outer 동시성 1.
- 집계: engine-started 10/10, 정상 요청 success 7/8(87.5%), 의도된 비정상 거부 2/2(100%), product failure 1, terminal/recoverable infrastructure failure·signal 0이다. child 실행시간 합계는 4,620,675ms다.
- 실행·검증 관측: discovery retry 8회, 후보 생성/고유 후보 355/355건, 평가 후보 633건, semantic/reference 등 후보 탈락 390건, 요청 시각 OPEN 검증 68건, URL 검증 68건, URL 검증 탈락 21건이다. 정상 반환 35개는 모두 고유 ID·지도 URL·요청 시각 `OPEN`을 가졌고, 외부 근거 URL은 33/35에 있었다. 비정상 2개는 정확한 `NONSENSE` reason 및 안정 오류 코드로 종료했으며 discovery/enrichment 호출은 없었다.
- 직접 검토: 반환 35개와 정상 실패 trace를 전수 읽고, 각 정상 실행 1위와 이상 항목의 지도·근거 URL을 외부 공개 정보와 대조했다. 독립 item 판정은 PASS 14/35(40.0%), WARN 16/35(45.7%), FAIL 5/35(14.3%)이고 hard defect는 3개, 동일 장소 중복은 0건이다. scenario 판정은 고정 곱창 WARN, 강남 카페 FAIL, 성수 파스타 FAIL, 이태원 비건 FAIL, 여의도 가족 한식 FAIL, 을지로 펍 FAIL, 다중 출발지 식사 WARN, 실내 활동 FAIL, 비정상 2건 PASS다.
- 식이 gate 결과: 이전 라운드에서 보인 일반 음식점·육류 중심 비건/할랄 false accept는 반환되지 않았다. 이태원 비건은 검증 식이 후보가 부족해 5회 재탐색 뒤 `EVALUATE_SEEDS_NO_RECOMMENDABLE_CANDIDATES` product failure로 종료했다. 이는 precision을 유지한 정상 요청 실패이며, 검증 gate를 완화해 채우지 않는다.
- 재현 hard defect: (1) 동일 상호의 다른 지점에 대한 Naver 검색 URL이 낮은 주소 일치에도 이름 점수로 신뢰되어, seed 좌표와 수 km 떨어진 지점의 영업·근거가 상위 후보에 결합됐다. (2) 맥주 펍 요청에 와인/젤라또 중심 바가 1위로 통과했다. (3) 활동 요청에 과거 전시 장소의 주차장 alias가 map-only 근거로 통과했다. 이 세 건은 장소 identity/업종 false accept다.
- 개선 판단: 라운드당 최소 변경과 `오수락 → 장소 identity` 우선순위에 따라 Round 7 전에는 전체 identity threshold를 올리지 않는다. 좌표/거리 proof가 없고 주소 score가 낮은 단일 Naver 검색 reference를 선택 근거로 쓰지 못하게 하며, 원래 상호·주소가 맞는 structured detail 또는 후보-entity 근접 좌표가 확인될 때만 통과시키는 좁은 reference identity 보정을 적용한다. 이 보정은 다른 지점 evidence 결합과 stale alias를 동시에 막고, 식이·영업·지도 hard gate를 완화하지 않는다. 맥주 펍 분류 보정과 비건 discovery coverage는 다음 독립 결함으로 남긴다.
- 누적 상태: counted 60/110. 정상 성공 37/48(77.1%), 의도된 비정상 거부 10/12(83.3%), terminal infrastructure failure 0/60, product failure 13이다. 반환 항목의 독립 품질 누적은 PASS 51/190(26.8%), WARN 58/190(30.5%), FAIL 81/190(42.6%)이며 Round 1은 반환 항목이 없어 이 분모에서 제외한다. Round 1 baseline 실패를 포함해 전체 acceptance 기준은 아직 충족하지 못한다.
- 다음 판단: Round 6에 infrastructure signal이 없으므로 Round 7 outer 동시성은 정책상 2로 시작한다. source는 다음 10회가 끝날 때까지 다시 동결한다.

### Round 6 이후 Round 7 사전 검증

- reference identity의 기존 전역 minimum score는 유지한다. 다만 거리/좌표 proof가 없고 주소 score가 0.8 미만인 단독 Naver search reference는 더 이상 map/evidence URL을 publish할 수 없다.
- 동일 후보에 대해 원래 상호와 함께 구조화된 entity 거리 250m 이하 또는 이름 score 0.82 이상·주소 score 0.8 이상의 detail evidence가 있어야 reference gate를 통과한다. 약한 기존 source URL도 재사용하지 않고 다시 detail resolution을 거쳐 증명되지 않으면 안전하게 탈락시킨다. 강한 Kakao detail이 있으면 약한 Naver URL만 출력에서 제외한다.
- 회귀는 R6의 같은 상호 다른 지점 충돌 reject, 120m structured entity+약한 Naver 경쟁 시 structured entity 유지, 정확 주소 Naver detail pass, 불완전 기존 Naver source의 재조회 후 안전 탈락을 포함한다. 식이·영업·기존 semantic gate는 변경하지 않았고, 중단한 주차장 suffix patch는 남아 있지 않다.
- 검증: recommendation-engine unit 54/54, recommendation-engine/server typecheck, campaign/CLI/review 64/64, 변경 파일 ESLint, `git diff --check`를 통과했다. 독립 코드 감사도 P0/P1 없음으로 판정했다. Round 7의 10회가 끝날 때까지 source를 동결한다.

### Round 7 — 2026-08-15

- run IDs: `place-regression-20260814.r07.*` 10건, source fingerprint `c885d17c7a29934984d7df876618e6bd1a1813e90e034d9720464d3624a7cd87`, outer 동시성 2.
- 집계: engine-started 10/10, 정상 요청 success 7/8(87.5%), 의도된 비정상 거부 2/2(100%), product failure 1, terminal/recoverable infrastructure failure·signal 0이다. child 실행시간 합계는 3,490,176ms다.
- 실행·검증 관측: discovery retry 6회, 후보 생성/고유 후보 333/333건, 평가 후보 498건, semantic/reference 등 후보 탈락 310건, 요청 시각 OPEN 검증 71건, URL 검증 74건, URL 검증 탈락 28건이다. 정상 성공 7건은 각각 고유 ID 5개·지도 URL·요청 시각 `OPEN`을 가졌고, 식이 제약 정상 요청은 검증 후보 1개만 반환되어 5개 계약 미달 product failure로 분류됐다. 비정상 2개는 각각 기대한 안정 오류 코드·reason으로 종료했고 downstream 장소 provider/evaluation 호출은 없었다.
- 직접 검토: 정상 반환 36개(완전 출력 7건의 35개와 식이 요청의 부분 출력 1개)를 전수 읽고, 각 정상 실행의 1위와 이상 항목의 지도·근거를 공개 정보로 대조했다. 독립 item 판정은 PASS 27/36(75.0%), WARN 7/36(19.4%), FAIL 2/36(5.6%)이며 hard defect 2개, 동일 장소 중복 0건이다. 지도 URL은 36/36, 출력 외부 근거 URL은 35/36에 있었다. fixed 곱창·성수 파스타·최소 입력·심야 술집은 전반적으로 적합했고, 조용함/업무 친화성 같은 긍정 근거 부족은 WARN으로 남겼다.
- identity 보정 확인: 이전 라운드의 동일 상호 다른 지점 근거 결합은 약한 단독 Naver search reference로 판정되어 출력에서 제거됐다. 이번 회전군에는 stale alias 재현군이 없으므로 그 결함의 완전 해소는 후속 회전 검증으로 남긴다.
- 재현 hard defect: (1) 명시 맥주·호프 요청에 후보별 양성 맥주 근거가 없는 와인·디저트 중심 바가 상위로 통과했다. 일반 심야 술집 의도에는 허용될 수 있으나 이 명시 제약에는 false accept다. (2) 다른 상호의 혼합 검색 문구에서 영업시간을 가져와 요청 시각 `OPEN`으로 잘못 판정한 후보가 있었다. 후자는 사실·영업 검증 결함으로 다음 우선순위에 남긴다.
- 개선 판단: 우선순위의 오수락을 먼저 처리한다. Round 8 전에는 요청이 명시적으로 맥주·호프·beer/pub/brewery·브루잉을 요구할 때만, 후보 ID/name/address에 귀속된 구조화된 category 또는 identity-qualified 개별 source의 양성 맥주 장소 근거를 요구하는 post-enrichment gate를 추가한다. aggregate search 문구·단순 map URL·일반 `bar`/심야 영업은 근거로 승격하지 않는다. 일반 심야 술집/이자카야 요청은 이 gate의 대상이 아니다.
- 누적 상태: counted 70/110. 정상 성공 44/56(78.6%), 의도된 비정상 거부 12/14(85.7%), terminal infrastructure failure 0/70, product failure 14다. 반환 항목의 독립 품질 누적은 PASS 78/226(34.5%), WARN 65/226(28.8%), FAIL 83/226(36.7%)이며 Round 1은 반환 항목이 없어 이 분모에서 제외한다. Round 1 baseline 실패를 포함해 전체 acceptance 기준은 아직 충족하지 못한다.
- 다음 판단: Round 7에 infrastructure signal이 없으므로 Round 8 outer 동시성은 정책상 3으로 시작한다. Round 8의 10회가 끝날 때까지 source를 동결한다.

### Round 7 이후 Round 8 사전 검증

- 명시적으로 맥주·호프·펍·beer/pub/brewery·브루잉을 요구하는 요청만 대상으로, post-enrichment와 reference URL/LLM scoring 사이에서 후보별 양성 맥주 장소 근거를 요구한다. 일반 `bar`·술집·이자카야·심야 영업 요청은 이 gate 대상이 아니다.
- 통과 근거는 후보 자체의 구조화된 상호·카테고리 또는 identity·주소가 모두 검증된 개별 Naver Search의 고정된 직접 제목 형태(`후보명 + 맥주 업종`)만 허용한다. Naver description, aggregate snippet, map URL, source detail은 근거로 사용하지 않는다.
- Naver title 근거는 동일 상호 다른 지점·같은 도로 다른 번지·번지 없는 후보 도로명·주소 불일치를 차단한다. 행정구역 표기 alias는 정규화해 동일한 상세 도로명·번지일 때만 허용한다. 부정·대안 표현(예: 맥주 제외)은 gate를 활성화하지 않는다.
- 회귀는 명시 맥주 요청의 일반 바 reject, 구조화된 호프/브루잉 및 직접 제목 positive pass, 일반 심야 바 보존, 잘못된 지점·번지·partial 주소 reject, description의 인근/이동/추천/소개/운영 문구 reject, 주소 alias 정상 pass, hard-reject 후보의 reference/scoring 미진입을 포함한다.
- 독립 코드는 외부 raw 검색 문구가 후보 속성으로 오인되지 않는지 재감사했고 P0/P1 없음으로 판정했다. 검증: recommendation-engine unit 57/57, recommendation-engine/server typecheck, campaign/CLI/review 64/64, 변경 파일 ESLint, `git diff --check`를 통과했다. Round 8의 10회가 끝날 때까지 source를 동결한다.

### Round 8 — 2026-08-15

- run IDs: `place-regression-20260814.r08.*` 10건, source fingerprint `74de43a91350c4cad3688ce8cd87369953de934af0824e28e4c902683c858adb`. outer 동시성 3으로 7건을 시작했고, 명시 OpenAI quota 신호 뒤 circuit breaker가 미시작 3건을 중단했다. 같은 source에서 명시 재개해 outer 동시성 1로 남은 3건만 실행했으며, 기존 7건은 재실행하지 않았다.
- artifact·집계: engine-started 10/10, artifact issue 0, 반환 26개, 중복 ID 0, Kakao map URL 26/26, 외부 evidence URL 25/26이다. child 실행시간 합계는 3,246,246ms, discovery retry 3회, 후보 생성/고유 후보 264/264건, 평가 후보 350건, 후보 탈락 241건, 요청 시각 OPEN·URL 검증 43/43건, URL 검증 탈락 25건이다.
- 원 manifest 분류는 normal success 5/8, intended rejection 0/2, product failure 5, terminal infra 0으로 기록됐다. trace와 오류 원문을 재감사한 보정 진단은 실제 product failure 1(식이 요청 1/5 반환), terminal OpenAI infrastructure failure 4(평가 1, initial plan 3), quota 영향 실행 6/10이다. 4건은 credit/billing exhaustion이지만 campaign classifier가 이를 quota 신호·infra로 인식하지 못해 product로 오분류했다. 비정상 2건은 intent 분류 전 initial LLM 호출이 실패해 의도된 거부를 평가하지 못했다.
- 직접 검토: 반환 26개를 전수 읽고 각 정상 실행의 1위·모든 이상 항목의 지도/외부 근거를 대조했다. 엄격 출력-사실 기준으로 PASS 9/26(34.6%), WARN 16/26(61.5%), FAIL 1/26(3.8%), hard defect 1개이며 top-3 hard defect도 1개다. 요청 시각 실제 이용 적합성만 별도로 보면 PASS 18/WARN 7/FAIL 1이다. 조용함·대규모 가족 세부 조건처럼 근거가 없는 소프트/미검증 설명은 WARN으로 유지했다.
- 재현 hard defect: 명시 1인 예산 상한보다 높은 candidate-bound 메뉴/가격의 최저가가 확인된 학생 식사 후보가 rank 3으로 통과했다. 현재 출력의 넓은 fallback 가격 범위를 신뢰해 reject하면 실제 저가 후보를 오거부할 수 있으므로, 주소·후보 identity에 결속된 개별 메뉴 가격 근거만 hard gate로 전달해야 한다.
- 기타 검토: 식이 요청의 유일 반환 후보 자체는 식이·장소·요청 시간에 적합하지만 5개 계약에는 미달했다. 맥주 펍 요청은 quota로 scoring이 붕괴해 R8 gate의 false-reject/false-accept 표본이 되지 못했다. 일부 영업시간/가격 출력은 공개 근거와 불일치했으나 요청 이용 시각에 실제 폐점한 항목은 이 라운드에서 확인되지 않아 WARN으로 기록했다.
- 개선 판단: Round 9 전에는 raw aggregate·카테고리 fallback을 쓰지 않고, identity·주소가 검증된 개별 메뉴/가격 source의 가격 claim만 보존해 명시 예산 상한보다 그 claim의 최저가가 높을 때만 post-enrichment, pre-reference/scoring `REJECT`하는 최소 gate를 추가한다. price unknown·겹치는 entry price·identity 불일치 근거는 reject하지 않는다. QA classifier의 credit exhaustion infra 오분류과 명백한 비장소 pre-LLM 거부는 독립 결함으로 후속에 기록한다.
- 누적 상태: counted 80/110. 원 manifest 기준 정상 성공 49/64(76.6%), 의도된 비정상 거부 12/16(75.0%), product failure 19, terminal infra 0이다. 보정 진단 기준 product failure 15, terminal infrastructure failure 4/80(5.0%)이며 R8의 비정상 2건은 infra 때문에 의도 거부 성능을 측정하지 못했다. 반환 항목 누적 독립 품질은 PASS 87/252(34.5%), WARN 81/252(32.1%), FAIL 84/252(33.3%)이다. Round 1 baseline과 누적 품질 때문에 전체 acceptance 기준은 충족하지 못한다.
- 다음 판단: R8 이후 확인된 OpenAI credit/billing exhaustion은 재시도로 회복할 수 없는 외부 상태다. R9 정적 검증은 진행하되, 정상 요청 live 호출은 credit 복구가 확인될 때까지 시작하지 않는다. 24시간 raw retention 정책과 최종 exact cleanup은 계속 유지한다.

### Round 8 이후 Round 9 사전 검증

- R8에서 재현된 명시 예산 상한 위반은 출력의 넓은 category/fallback 가격 범위가 아니라, 후보 identity·주소에 결속된 개별 메뉴 가격이 누락되어 발생했다. 이에 내부 `verified price claim`을 추가했고, source URL·identity/address score·확인된 최소 가격을 함께 보존한다.
- 가격 claim은 identity·주소가 모두 검증된 개별 source에서만 만들고, 후보 직접 제목과 중립적인 메뉴/가격 field 문맥이 함께 확인될 때만 허용한다. aggregate snippet, category/display fallback, 목록·근처·비교·다른 장소 문맥, 상호 접두/접미 충돌, 혼합 지점 주소는 fail-closed로 제외한다.
- 예산 hard gate는 `verified price claim`만 읽으며, 명시 1인 예산 상한보다 확인된 최소 가격이 높을 때만 reference URL/LLM scoring 전에 `REJECT`한다. 가격 미상·상한과 겹치는 entry price·identity 불일치 정보는 reject하지 않는다.
- 회귀는 후보 직접 가격의 상한 초과 reject, 실제 저가·가격 미상·겹치는 entry price pass, 목록/근처/다른 장소/상호 부분일치/혼합 branch 가격의 claim 미생성, reject 후보의 reference/scoring 미진입을 포함한다. 독립 재감사에서 위 오염 반례와 정상 가격 경로를 모두 재현해 P0/P1 없음으로 판정했다.
- 검증: recommendation-engine unit 59/59, recommendation-engine typecheck, server typecheck, campaign/CLI/review 64/64, R9 변경 파일 ESLint, `git diff --check`를 통과했다. R9 live 호출은 0건이며 counted 상태는 80/110으로 유지한다.
- 다음 판단: OpenAI의 명시 credit/billing exhaustion이 해소되기 전에는 R9 정상 요청을 시작하지 않는다. 복구 후 새 source fingerprint로 Round 9을 시작하고, 10건 전수 검토 뒤에만 다음 최소 개선을 선택한다.

### Round 9 — 2026-08-15

- run IDs: `place-regression-20260814.r09.*` 10건, source fingerprint `9c52c2c682a015f4a286e196975f4d14939878c8f44c1a3e95ea47e1eb7bb9e1`, outer 동시성 1. R8의 provider signal 이력으로 자동 유도된 값이며 outer automatic retry는 사용하지 않았다.
- 집계: engine-started 10/10, 정상 요청 success 7/8(87.5%), 의도된 비정상 거부 2/2(100%), product failure 1, terminal/recoverable infrastructure failure·signal 0이다. 반환 추천은 36개, 중복 ID·artifact issue는 0이며 추천마다 지도·외부 근거를 확인할 수 있었다.
- 실행·검증 관측: 총 child 실행시간 2,572,573ms, discovery retry 3회, 후보 생성/고유 후보 269/269건, 평가 후보 330건, 후보 탈락 213건, 요청 시각 OPEN 검증·URL 검증 53/53건, URL 검증 탈락 27건이다. R8에서 발생한 OpenAI credit exhaustion은 재발하지 않았다.
- 비정상 요청 검토: 두 요청 모두 `UNSUPPORTED_RECOMMENDATION_REQUEST`와 `NON_PLACE_REQUEST` reason으로 추천 0건을 반환했다. 각각 `process.start → intent_classified → unsupported_request → process.failure`만 남고 discovery·평가·enrichment·장소 provider 호출이 없었다.
- 식이 정상 요청 검토: 1/5만 반환되어 scenario는 coverage FAIL이지만, 유일 반환 항목은 identity·식이 근거·요청 목요일 저녁 영업이 확인돼 precision PASS다. 추가 후보는 폐점/영업 미확인/지역·식사·식이 충돌로 탈락했고 hard gate를 완화해 채우지 않았다.
- 직접 검토: 반환 36개를 전수 읽고 모든 정상 실행의 1위와 이상 항목의 지도·외부 근거를 대조했다. 독립 item 판정은 PASS 10/36(27.8%), WARN 22/36(61.1%), FAIL 4/36(11.1%)다. review의 `MISSING_BUDGET` structural flag는 array-shaped 가격 범위를 잘못 읽은 QA 도구 false positive여서 item 품질 결함으로 집계하지 않았다.
- 재현 결함: 명시 해산물 요청에 후보 자체의 해산물 양성 신호가 없는 일반 중식이 통과한 명백한 업종 false accept가 확인됐다. 그 밖에 명시 조용함과 충돌하는 카페 1위, 지역 요청과 크게 떨어진 fallback 곱창 후보, 업무 카페의 Wi-Fi·콘센트 근거 부족 및 먼 후보, 일부 break-time/가격 표시 불일치는 각각 FAIL/WARN으로 기록했다. 명시 맥주 펍 후보는 모두 실제 맥주 업종으로 확인됐다.
- 개선 판단: 우선순위 `오수락`과 라운드당 최소 변경 원칙에 따라 Round 10 전에는 해산물 false accept만 수정한다. 조용함·거리 fallback·업무 편의 근거·영업 projection·식이 candidate coverage·QA structural false positive는 별도 결함으로 남긴다.
- 누적 상태: counted 90/110. 정상 성공 56/72(77.8%), 의도된 비정상 거부 14/18(77.8%), 보정 진단 기준 product failure 16, terminal infrastructure failure 4/90(4.4%)다. 반환 항목 독립 품질 누적은 PASS 97/288(33.7%), WARN 103/288(35.8%), FAIL 88/288(30.6%)이며 Round 1은 반환 항목이 없어 이 분모에서 제외한다. Round 1 baseline과 누적 품질 때문에 전체 acceptance 기준은 아직 충족하지 못한다.
- 다음 판단: R9에 infrastructure signal이 없으므로 Round 10 outer 동시성은 정책상 2다. R10의 10회가 끝날 때까지 source를 동결한다.

### Round 9 이후 Round 10 사전 검증

- 명시 해산물/해물/seafood 요청에서 후보 자체의 구조화된 상호·카테고리·tag가 일반 중식/중화 계열이고 해산물 양성 신호가 없을 때만 semantic `REJECT`한다. 기존 햄버거·패스트푸드 충돌 rule은 유지한다.
- 후보 자체에 해물·해산물·생선·수산·조개·장어·굴·전복·낙지·문어·쭈꾸미·대게·랍스터·복어·횟집·사시미·초밥 신호가 있으면 중식이어도 통과한다. 희소 후보, 명시적 한/영 대안, 해산물 부정 요청은 reject하지 않으며 raw snippet·LLM·URL은 근거로 쓰지 않는다.
- 회귀는 generic Chinese reject, positive Chinese seafood pass, sparse pass, 한/영 대안·부정 pass, raw snippet-only positive 차단, reject 후보의 reference URL/LLM scoring pool 미진입을 포함한다. 독립 감사에서 P0/P1 없음으로 판정했다.
- 검증: recommendation-engine unit 59/59, recommendation-engine/server typecheck, campaign/CLI/review 64/64, 변경 파일 ESLint, `git diff --check`를 통과했다.

### Round 10 — 2026-08-15

- run IDs: `place-regression-20260814.r10.*` 10건, source fingerprint `a6646c213fad6d72bba2922bad08c1a9249144b2876445e1962b56b1af829e5f`. outer 동시성 2로 7건을 시작했고, 서로 다른 두 실행에서 반복된 OpenAI transport/DNS abort signal로 circuit breaker가 열렸다. 같은 source에서 명시 재개해 동시성 1로 미시작 3건만 실행했으며, 기존 7건을 재실행하지 않았다.
- 집계: engine-started 10/10, normal success 6/8(75.0%), intended unsupported rejection 2/2(100%), product failure 2, terminal infrastructure failure 0이다. 성공 결과를 포함한 infrastructure-affected run은 2/10(OpenAI)이며, circuit acknowledgement와 raw artifact 일관성 검증을 통과했다.
- 실행·검증 관측: 정상 성공 6건은 각각 고유 ID 5개·지도 URL·요청 시간 `OPEN`을 가졌다. 비정상 2건은 기대한 오류 코드와 `NON_PLACE_REQUEST`/`CONTRADICTORY_REQUEST` reason으로 종료했고 discovery·평가·장소 provider 단계가 실행되지 않았다. 식이 복합 정상 요청과 고정 비건 정상 요청은 각각 검증 가능한 후보를 5개 확보하지 못해 `EVALUATE_SEEDS_NO_RECOMMENDABLE_CANDIDATES`로 종료했다.
- 직접 검토: 반환 30개를 전수 읽고 각 정상 성공의 1위와 모든 이상 항목의 지도·외부 근거를 직접 대조했다. 독립 item 판정은 PASS 16/30, WARN 11/30, FAIL 3/30, hard defect 2개다. 브레이크타임 경계 시나리오는 5개 모두 요청 체류시간 영업 조건을 충족했고, 비정상 두 건은 조기 거부와 downstream 미호출을 확인했다.
- 재현 결함: 90분 체류가 필요한 명시 조용한 카페 요청에서 좌석이 없는 테이크아웃형 후보가 1위로 수락됐다. 이는 근거 부족만이 아니라 후보 결속 외부 근거가 체류 조건과 직접 충돌하는 false accept다. 그룹 메뉴 가격을 1인 가격으로 표기한 사실 오류와 실질 중복 가능성, 식이 제약의 verified candidate coverage 부족은 별도 결함으로 남긴다.
- 개선 판단: 우선순위 `오수락`에 따라 최종 10회 전에는 명시 조용함·체류 요청에서 후보 결속 근거가 `좌석 없음` 또는 `테이크아웃 전문`을 명시할 때만 해당 후보를 pre-reference/scoring 단계에서 reject하는 최소 보정을 검토한다. 체인 전체 차단, 긍정 근거 부재만의 reject, identity·영업·식이 hard gate 완화는 하지 않는다. 구현 전 candidate-bound evidence 경로와 false-reject 반례를 독립 검토한다.
- 누적 상태: counted 100/110. manifest 기준 normal success 62/80(77.5%), intended unsupported rejection 16/20(80.0%), product failure 22, terminal infrastructure failure 0, infrastructure-affected run 14/100이다. Round 1 baseline 및 누적 품질로 인해 전체 acceptance 기준은 아직 충족하지 못한다. 최종 10회는 별도 manifest와 수동 scenario 통계를 사용하며, 실패 시 추가 live 호출은 하지 않는다.

### Round 10 이후 최종 검증 사전 보정

- R10의 좌석 없는 카페 false accept는 최우선으로 설계·독립 감사했다. 그러나 엔진이 보관한 candidate-bound 개별 source는 좌석 부재가 아니라 상충하지 않는 문구였고, 다른 후보의 포장·모바일주문 문구를 근거로 삼으면 nearby/aggregate contamination과 false reject가 된다. 따라서 체인·일반 takeout·긍정 근거 부재를 hard reject하지 않았고, 명확한 후보 결속 `NoSitInClaim` source가 확보될 때까지 이 결함은 WARN/후속으로 기록한다.
- 다음 재현 가능한 높은 우선순위는 식이 정상 요청의 discovery starvation이다. 희소 식이 요청의 broad fallback이 일반 `맛집`으로 들어가면 후보 pool의 상위 enrichment window가 일반 식당으로 채워져 verified dietary candidate가 밀려난다.
- 변경: broad fallback은 비건·할랄 제약을 일반 음식/카페/술집 fallback보다 먼저 보존한다. 비건은 `비건 식당`, 할랄은 `할랄 식당`, 두 제약이 모두 명시되면 `비건 할랄 식당`으로 검색한다. 이 변경은 discovery 후보군만 보정하며 dietary identity·ALL/ANY·영업·URL·가격·reference hard gate를 완화하지 않는다.
- 안전성 재감사: 초기 구현에서 비건/할랄 카페·펍 요청이 일반 업종 fallback으로 희석되는 P1을 발견해 우선순위를 보정하고 해당 조합 회귀를 추가했다. `비건 또는 할랄`처럼 대안 의미를 가진 별도 문구는 final fixture 범위 밖이라 이 최소 변경에 섞지 않고 후속 P2로 남긴다.
- 검증: recommendation-engine unit 60/60, recommendation-engine/server typecheck, campaign/CLI/review 64/64, 변경 파일 ESLint, `git diff --check`를 통과했다.
- final scenario stats: 100회 manifest와 수동 검토를 대조해 raw campaign root에 26개 scenario의 정확한 실행/실패 수를 기록했다. 실패율 50% 동률 edge 중 수동 품질 점수는 비건+할랄 0, 실내 데이트 25, 다중 출발지 35, 8인 가족 35으로 확정했다. 따라서 final 10은 고정 정상 6 + `edge_vegan_halal_meal` + `edge_indoor_activity_date` + `unsupported_emoji_only` + `unsupported_nonsense`로 고정한다. 비정상 동률은 같은 수동 품질 기준으로 구분할 반환 품질 차이가 없어 안정 ID 순서를 사용한다.
- 다음 판단: R10에서 recoverable infrastructure-affected run이 있었으므로 final은 adaptive outer 동시성 1로 시작한다. R1 시작시각부터 24시간 한도 내에서만 새 child를 시작하고, final 실패 시 추가 live 호출·범위 확장은 하지 않는다.

### 최종 검증 (Round 11) — 2026-08-15

- run IDs: `place-regression-20260814.r11.*` 10건, source fingerprint `aec7370d944904d329102b59f5180fafd0dd4c1313514558a6ca1b6a97c39e22`, outer 동시성 1. final scenario stats의 고정 검증을 통과한 뒤 고정 정상 6, 최저 수동 품질 점수 edge 2, 비정상 2를 정확히 한 번씩 실행했다.
- accounting/artifact: engine-started 10/10, not-counted 0, duplicate execution 0, manifest·lifecycle·result·log·events 참조 불일치 0이다. 반환 추천은 31개, unique engine ID 31개, Kakao map URL 31/31, 요청 시각 `OPEN` 31/31, 외부 근거 URL 보유 30/31이다. automatic review packet은 artifact issue 0, discovery retry 8, candidate 311/unique 311, evaluated 395, rejected 243을 기록했다.
- outcome: 정상 요청 success 6/8 (75.0%), 의도된 비정상 거부 2/2 (100%), product failure 2, terminal infrastructure failure 0이다. `itaewon_vegan`은 검증 가능한 후보 1/5, `edge_vegan_halal_meal`은 0/5로 정상 요청 coverage 조건을 충족하지 못했다. 후자의 raw trace에는 단일 recoverable operation-hours provider failure가 있었지만 terminal 원인으로 단정하지 않았고 manifest terminal infrastructure failure로도 집계하지 않았다.
- 비정상 검토: 두 실행은 각각 `NONSENSE` reason과 `UNSUPPORTED_RECOMMENDATION_REQUEST`로 종료했다. event 순서는 `process.start → intent_classified → unsupported_request → process.failure`뿐이었고 discovery/evaluation/enrichment/장소 provider 단계는 없었다.
- 직접 검토: 31개 반환 항목과 정상 실행의 모든 1위·이상 항목을 map/evidence와 대조했다. 독립 item 판정은 PASS 16/31 (51.6%), WARN 10/31 (32.3%), FAIL 5/31 (16.1%)이다. hard-defect cluster는 3개였다. (a) 한 부분 성공 식이 후보는 장소·식이·요청 시각 자체는 맞았지만, 반환된 외부 근거 중 한 개가 다른 지점으로 연결됐다. (b) 맥주 펍 결과에서 서로 다른 두 후보가 동일 canonical map reference를 공유했다. (c) 실내 `ACTIVITY` 데이트 결과의 일반 카페 2개는 장소·영업은 맞아도 150분 활동형 데이트의 활동 근거가 없었다. 영업시간 표시가 실제 요청 체류에는 문제 없지만 공개 정보와 불일치한 2개 항목, 조용함/체류·가족 세부 근거 부족은 WARN으로 남겼다.
- scenario 판정: 회기 곱창 PASS, 강남 조용한 카페 WARN, 성수 파스타 WARN, 이태원 비건 FAIL(coverage와 reference branch), 여의도 가족 한식 WARN, 을지로 펍 FAIL(map identity collision), 비건·할랄 식사 FAIL(coverage), 실내 데이트 FAIL(activity mismatch), 비정상 2건 PASS다. hard defect가 반환 top-3에 포함되어 final top-3 hard-defect 0 조건도 충족하지 못했다.
- acceptance: 100회 본선 manifest 기준 정상 성공 62/80 (77.5% < 90%), 의도된 비정상 거부 16/20 (80% < 100%), terminal infra 0/100이다. R8 credit exhaustion의 사후 trace 재분류는 terminal OpenAI infra 4/100으로 보정되지만 success/rejection 합격 여부를 바꾸지 않는다. R9 누적 독립 item 품질 97/288 PASS에 R10 16/30 PASS를 합산하면 본선 PASS는 113/318 (35.5% < 90%)이다. final은 정상 6/8로 요구한 8/8에 미달하고 top-3 hard defect도 존재한다. 따라서 캠페인 합격선은 **FAIL**이다.
- final 이후 조치: 사용자 계약에 따라 추가 live 호출, 성공률을 위한 hard gate 완화, 범위 확장은 하지 않았다. 후속 비-live 개선 후보는 (1) candidate별 reference URL의 branch identity filter, (2) joint dietary constraint의 discovery coverage/priority 보강, (3) `ACTIVITY` 요청에서 generic cafe를 활동 장소로 승격하지 않는 evidence-aware ranking/gate, (4) canonical map identity collision 제거다. 조용함/체류 조건은 candidate-bound 명시 부정 근거가 확보될 때만 hard gate로 다룬다.
- final verification: recommendation-engine unit 60/60, campaign/CLI/review 64/64, recommendation-engine/server/campaign typecheck, 변경 파일 ESLint, `git diff --check`를 최종 source에서 재실행해 통과했다. raw campaign root는 11 manifest, 110 engine-started, 452 regular files (180,804,795 bytes), lock 0, symlink 0, 참조 artifact issue 0을 확인했다. 이 추적 로그에는 raw provider 응답·주소·URL을 보존하지 않는다.
- cleanup: 위 검증을 같은 foreground shell에서 다시 수행한 뒤 campaign raw root 하나만 삭제했고, 삭제 후 경로 부재를 확인했다. raw artifact는 ignored 디렉터리였으며 이 정리는 되돌릴 수 없다. 추적 로그와 엔진·QA 코드, 다른 `.log` 경로 및 사용자의 기존 변경은 삭제하지 않았다.
